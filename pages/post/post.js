/*
import { useRouter } from "next/router";

const Post = () => {
  const router = useRouter();

  return (
    <div className="bg-gradient-to-br from-color1 via-color2 to-color3" style={{height: '100vh'}}>
      <article className="text-white">
        <h1>{router.query.slug}</h1>
      </article>
    </div>
  );
};

export default Post;
*/